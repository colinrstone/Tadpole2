#include "GameState.h"

// Constructor implementation
GameState::GameState(int initialZone, int initialHealth, int initialGold) :
    zone(initialZone),
    playerHealth(initialHealth),
    playerGold(initialGold) {
    // Initialize other game state variables here
}